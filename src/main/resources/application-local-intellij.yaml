spring:
  config:
    import: ""

  datasource:
    url: jdbc:postgresql://localhost:5432/followme
    username: followme
    password: followme

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
